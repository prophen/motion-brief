# Placeholder

Pre-cutover create-build-service images assert `references/*.md` exists at
image build; this file keeps that guard green. Delete it once the relaxed
guard (SKILL.md only) is everywhere. Documentation: <https://docs.deep.space>.
